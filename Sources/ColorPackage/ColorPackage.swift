public struct ColorPackage {
    public private(set) var text = "Hello, World!"
    public private(set) var hello = "Hello, World!"
    public private(set) var new = "Hello, World!"

    public init() {
    }
}
